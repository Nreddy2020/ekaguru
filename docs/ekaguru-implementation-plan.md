# EKAGURU implementation plan

Updated 11 September 2026. This is the durable plan for continued implementation, not a claim of global product completeness.

## Verification gate
Define acceptance before each stage; implement, test the appropriate unit and actual integration/UI behavior, inspect and repair failures, then record evidence and limits before proceeding to dependent work. Pending or mocked verification is not live runtime proof. A blocked stage permits only genuinely independent authorized work. Routine user approval is not required.

## Intended outcome
A learner opens their own textbook page in the already approved EKAGURU classroom. Guru teaches that exact page with progressive writing, drawings, spoken explanations, level-appropriate reasoning and checkpoints. The original source remains available, and progress or mastery claims require recorded evidence.

## Architecture decisions
- Preserve the approved library/classroom references in approved-interface-architecture.md.
- Carry book ID, physical page, source hash and learner preferences through extraction, planning, runtime and source highlights.
- Keep model provider credentials on the server. Validate model output and evidence IDs before presenting or persisting it.
- Keep page-practice observations separate from canonical mastery until concept mapping is verified.
- Preserve originals. Never substitute an EVS sample for an unavailable upload.
- Store local PDFs in IndexedDB; distinguish device-local originals from authorized server materials.
- Continue existing Gemini support while implementing the user's requested OpenAI option through the same model boundary.

## Milestones and acceptance (status as of 11 September 2026, evening)
1. Source-grounded pipeline and durable runtime: implemented and live-verified for EVS page 46 at all five depths with Gemini. Durable job queue, persisted page evidence, cacheable image delivery and per-account budgets added.
2. Approved UI integration and Open Book repair: implemented; small-screen jump links added.
3. Configurable provider: Gemini live-verified; OpenAI path still only mocked (no key).
4. Authenticated upload bridge for local PDFs: implemented and verified end to end (byte identity, owned upload, page evidence, signed image links); verified authentication, recovery and email verification in place. Remaining: password recovery mail needs a real delivery webhook; first-time OCR of an uploaded scan still runs inside the request.
5. Educator-reviewed teaching quality and canonical concept mapping: infrastructure complete (rubric, corpus of 185 cases, curator pages, concept-mapping panel, gated mastery bridge). Blocked on people and money: no educator score exists yet, and the Gemini free tier (20 requests per day) prevents preparing the corpus; the bridge therefore stays closed.
6. Production hardening: partly done (rate/cost limits, durable jobs, evidence cache, mobile reflow, honest mastery statements, full suites green). Remaining: queued OCR for first-time uploads, job cancellation, load tests on a production build with replicas, accessibility audit with a screen reader, deployment baseline of the database migration history, mail delivery, tested speech voices and localization.
7. Learning-science loops: spaced review of practised pages implemented; still missing from the PRD are the reflection and teach-back agent, transfer testing, fear and confidence signals, and connecting the Python diagnosis loop to the page board.
8. Outcome evidence: not started. A learner pilot with pre and post measures and an educator-reviewed corpus are what would justify any "world reference" claim.

## Current evidence
- Full frontend: 23 suites, 113 tests pass; existing PDF-viewer mock warnings remain.
- Focused Guru backend runtime: 20 tests pass, including grounded page questions that do not pass a checkpoint.
- Backend build and frontend TypeScript pass.
- Earlier full backend: 53 of 54 suites pass; three assertions in the legacy phase4 Socratic fixture remain incompatible with unchanged production behavior. See page-teaching-validation.md.
- Real local-PDF check: generated classroom-test-source.pdf (two pages) uploaded through the existing modal and verified through normal Open Book. Page two taught plant content and blocked Next until the source answer. The created library entry is explicitly named classroom-test-source.
- Standalone Puppeteer harness: could not start Chrome within its launch timeout; not counted as passed. The actual flow was exercised through the in-app browser instead.
- In-app browser: normal EVS page 46 displays the original scan and approved classroom. The reported book-1788352249934 displays the approved shell and an attach-original recovery action, without the incorrect sign-in prompt.

## Blockers and limits
No Gemini/OpenAI provider key is configured in this environment. Paid live reasoning has not been exercised. The old upload discarded the source file, so its contents cannot be recovered from metadata; the original PDF is required. Local scanned pages currently display the scan but cannot receive source-reading lessons until readable evidence exists. No production deployment, data reset, model spending, or release publication has been performed.

## Coordination
An attempted milestone report to the separate VITALIS and FIN Mobile Status task was blocked by automatic approval review because sharing authorization was not verified. No status was transmitted. Continue to record evidence locally; obtain verified authorization before another send.

## Next stage acceptance: upload source identity
Before connecting local PDFs to the server: uploads with identical metadata and length but different bytes must remain distinct; identical bytes may reuse an owned material; inaccessible/missing old source files must never be treated as confirmed duplicates; existing upload authorization must remain enforced. Run focused upload HTTP/service checks and an actual storage/database fixture before marking this prerequisite complete.

Source-identity prerequisite PASSED: 17 upload service/HTTP tests passed. scripts/verify_upload_source_identity.js exercised actual PostgreSQL and filesystem storage with valid PDFs of identical names, titles and lengths but different bytes; distinct content created distinct materials, identical content reused the existing material, and a missing old original preserved a new upload. Cleanup removed only the generated fixture rows/files.

## Next stage acceptance: local-to-server bridge
No PDF transfer on page open. An explicit Connect to Guru action must require a signed-in account and a learner selected from its authorized list. Enforce the existing 50 MB server limit while preserving 200 MB local reading. Upload the original bytes, compare the returned SHA-256 checksum, and open only the returned material ID at the same physical page. Missing credentials, denied access, oversize files, malformed responses and cancellation must preserve local reading. Verify component behavior and an actual authenticated multipart-upload to page-evidence HTTP path. Live model teaching remains a separate blocked gate.

Bridge gate update: byte identity and owned JWT HTTP upload/page rendering passed, but identity verification is NOT ready. The /login UI simulates login; AuthService accepts any nonempty password for fixed admin/demo emails and existing parent emails. This invalidates a claim of secure private-book access. Before enabling the user connection flow, replace this path with password verification, remove public role escalation/automatic account creation, reject legacy unverified tokens, and verify the actual registration/login/token behavior. Additive password-hash migration must preserve existing parent/learner records; legacy accounts without a verified credential must not be auto-claimed. No live model calls or external identity services are authorized.

Authentication prerequisite implemented: password hashes are stored separately in ParentCredential; public registration cannot choose ADMIN; unknown accounts are not created during login; legacy accounts without credentials cannot be claimed; JWT authVersion=2 rejects old unverified tokens. The local ignored backend environment now has a random signing secret, and non-test startup rejects weak placeholders. Existing records were preserved with one additive table. Password recovery and email ownership verification are still unimplemented, so legacy-account recovery remains pending. HTTP credential and original-upload tests passed against real PostgreSQL/storage; full authenticated browser verification is next.

## Stage completed 11 September 2026: quality gates, mastery bridge, budgets, recovery
Acceptance defined in advance: (a) canonical mastery can change only through an explicit, tested gate chain; (b) an educator evaluation corpus exists with a versioned rubric, real page cases and a working review path; (c) paid model calls are budgeted per account; (d) legacy accounts can be claimed only by proving email ownership, and a reset revokes older sessions; (e) the fabricated demo teaching code is gone from the frontend; (f) every change has unit coverage plus a real-database check where the behaviour depends on the database.

Implemented (see guru-quality-and-mastery-gates.md for the operating description):
- `GuruUsageService` daily ledger with atomic reservation; planner reserves budget only on cache miss; 429 with source reading preserved.
- `GuruConceptMappingService` (page-range and objective-name proposals, ADMIN-only verification, manual links) and `GuruMasteryBridgeService` (four gates, deterministic evidence keys, EXPLANATION evidence through the existing mastery calculator). The bridge is disabled by default; assessed answers report `masteryUpdated`, `conceptIds` and `masteryNote`, and the board shows the learner what changed.
- Evaluation corpus: rubric v1 with gate criteria, `GuruEvaluationService`, curator controller under `/api/v2/guru/evaluation`, Markdown review packets, `/admin/guru-evaluation` review page, and a 185-case v1 manifest over 37 real built-in pages (`docs/evaluation/guru-evaluation-corpus.v1.json`, imported into the local database by `scripts/seed_guru_evaluation_corpus.js`).
- Recovery: `ParentRecoveryToken` (hashed, single-use, expiring), `RecoveryService`, `MailerService` webhook boundary, `credentialVersion` claim checked by the JWT strategy, `/login/recovery` page, and email verification on registration.
- `RolesGuard` now honours class-level role metadata; before this fix a class-wide ADMIN restriction was ignored (found by the new real-database curation spec).
- Test-harness repairs: integration specs sign with the shared `authSigningSecret()`; real-database specs restore `JWT_SECRET` before cleanup, so a database outage no longer cascades into unrelated 401 failures.
- Frontend clean-up: the demonstration studio body, `LivingGuruBlackboard`, `BookPageViewer` and six fabricated engine/service files were deleted (about 4,800 lines); routes keep working through the alias export.
- Database: the pre-existing schema was baselined (`migrate resolve --applied 20260809000000_legacy_v1_baseline`) and the three additive migrations deployed to the local `cognitive_memory` database. No reset, no data change.

Evidence: backend `npm test` 63 of 64 suites, 390 of 393 tests; the only failure remains the legacy phase4 Socratic contract mismatch already documented in page-teaching-validation.md. Frontend 28 suites, 130 tests. Backend and frontend TypeScript pass. Real-database checks passed for: verified registration/login, legacy-account recovery with session revocation, owned local-PDF upload to page evidence, curator role enforcement and corpus import, and the Phase 2.9 session runtime.

Still blocked or pending, unchanged in kind: no provider key, so no lesson has been generated for the corpus and no educator score exists; the mastery bridge therefore cannot record evidence for any depth. Mail delivery has only the in-memory test transport. HawkScan could not run in this workspace (CLI not installed, no credentials, Docker stopped). The legacy Socratic suite still needs a contract decision.

## Stage completed 11 September 2026 (later): first live Guru lessons
Acceptance defined in advance: (a) a real multimodal lesson for a real scanned page is accepted by the unchanged server validators and plays in the approved classroom; (b) a learner answer is graded live and the checkpoint releases; (c) every failure met on the way is either fixed in code with a test or recorded as a known limit; (d) the provider key lives only in the ignored backend `.env`.

Live results with Gemini (`gemini-2.5-flash`; the newer `gemini-3.8-flash` answered probes but returned 503 high-demand for real lessons):
- EVS Class 5 page 46 at basis depth: accepted after one repair pass, 17 actions covering 14 of 15 vision blocks with the page number explicitly omitted, model review passed, about 117 s. Advanced depth: accepted, 17 actions, about 172 s. Both are stored as corpus artifacts for educator review.
- In the in-app browser the basis lesson played on the approved board and the checkpoint "What kind of things do people usually buy at a market?" blocked Next. A typed learner explanation was graded live: criteria met 2 of 2, confidence 1.0, feedback shown on the board, Next released, progression to action 7 of 17 confirmed. The assessment carried `masteryUpdated:false` with note `bridge-disabled`, and the board now renders that note in plain language.
- Failures found and fixed: 12k output-token cap truncated transcriptions (now 32k, configurable); 90 s deadline aborted deep and developing plans (now 180 s, configurable); free-tier 429 throttling under concurrent generation (bounded retry, at most two waits); plans that skipped a page-number block, omitted rectangle heights, emitted empty notes or empty action text (explicit `omitted` list with reasons, Gemini structured output on planning, grading and page questions, clamped scene geometry, one validator-guided repair pass, notes defined in the prompt); a whole page rejected for one low-confidence region (uncertain regions are now set aside, reviewer-visible, capped at a quarter of blocks); a grading reply rejected for shape (structured output plus a content-free shape diagnostic in the server log). The vision transcription is cached per page image and model so five depths share one vision call.
- With the hardened code, developing (16 actions, about 162 s) and deep (16 actions, about 218 s) were accepted for page 46. Proficient hit the 32k output-token cap on one call, then the 180 s deadline; the default cap was raised to 48k (Gemini 2.5 Flash allows 65,536) and it was accepted on the third attempt with a 300 s deadline (21 actions, about 149 s). All five depths of page 46 are now prepared for educator review. Every attempt, success or failure, is appended to `docs/evaluation/prepare-results.jsonl`.

Depth differentiation observed on the same page: the basis lesson's checkpoints ask recall-level questions ("What kind of things do people usually buy at a market?", rubric of 2 criteria), while the advanced lesson's checkpoints ask "Why are markets important?", "What are the broader benefits of parks?" and "How do banks support the economy?" with 3 criteria each and objectives about economic and social roles. This is the first live evidence that depth can change reasoning demand. It is not yet consistent: the accepted deep lesson's checkpoints ("Why do people go to a market?", "Do you have friends in your neighbourhood?") are no more demanding than the developing lesson's, so the model did not honour the first-principles instruction for deep on this page. That inconsistency is exactly what the educator corpus must measure (pedagogical progression criterion) before any depth is approved; the planner prompt is deliberately left unchanged until the v1 corpus has been scored, so results stay comparable.

Limits: these are engineering acceptance results, not educator judgements; no rubric review has been recorded yet. The free tier throttles concurrent generation, so corpus preparation must run serially. The API key that was pasted in chat had already been revoked by Google as leaked before use; the replacement was entered directly into `.env` and must be rotated if it ever appears in a transcript or log.

## Findings and additions after the first live day (11 September 2026, later)
- Free-tier limit is the real blocker for the corpus: Google's quota for `gemini-2.5-flash` on this project is 20 requests per day (`GenerateRequestsPerDayPerProjectPerModel-FreeTier`). One lesson needs 3 to 4 calls, so the free tier yields at most about five lessons a day per model. The 25-case pilot batch failed every case within 47 seconds each once the quota closed. The model boundary now recognises a per-day quota violation, does not retry it, and returns a distinct message; the corpus script stops the batch on that message instead of burning through the remaining cases. Preparing the 185-case corpus (roughly 600 to 750 calls) requires pay-as-you-go billing on the Google AI Studio project or a paid provider; the code path is otherwise proven.
- Learner attribution for built-in textbooks: a signed-in parent can now choose a learner profile in Guru settings; the session is bound to that learner after ownership verification, sessions are kept separate per learner, private materials still bind only to their own learner, and a mismatching request is refused. This removes the "no-learner" gate for built-in books and closes the "learner selector for shared parent accounts and builtin books" requirement from the architecture review. Covered by runtime and studio tests, and verified in the in-app browser: a disposable parent with two learner profiles opened page 46, chose one learner in Guru settings, and the server created a second session bound to that learner while the unattributed session stayed separate (confirmed in the database).

Evidence at the end of the day: backend 63 of 64 suites, 396 of 399 tests (legacy Socratic contract test only); frontend 28 suites, 132 tests; TypeScript clean on both sides.
- Curator concept-mapping panel: the evaluation page now lists a lesson's concept mappings, proposes links from extraction provenance, verifies or rejects each one, and links a canonical concept manually through an ADMIN-only concept search (`GET /api/v2/guru/concepts?search=`). Verified live on page 46 (basis): the local corpus has no extraction provenance for built-in scans, so "Propose from extraction" correctly produced nothing, and the manual search found the seeded concepts "Community Helpers" and "Community Harmony". One manual link was made on the local database as a demonstration; it is a curator decision on dev data, not an educator judgement.
- Durable lesson generation (production hardening milestone, first item): lesson requests now return a cached lesson or a 202 job instead of holding the HTTP connection for two to four minutes; a compare-and-swap worker generates in the background with bounded attempts, stale-job recovery and stage reporting; the classroom and the curator page poll the job and show the stage. Curator "Prepare lesson" also queues. Unit coverage for enqueue/dedupe/claim/retry/recovery/concurrency and for client polling, failure and abort. See guru-quality-and-mastery-gates.md, "Durable lesson generation".
- Parent visibility (PRD principle "explainable AI for parents"): a per-learner Guru activity endpoint and a dashboard section derived only from the session ledger, with the counting rules stated on the page. Verified live: a parent-owned learner's session showed progress 7 of 17, one assisted checkpoint and zero independent passes, and a stranger's token was refused with 403.
- Legacy Socratic suite brought to the current contract, and one honesty bug fixed on the way: the Socratic tutor announced "Mastered!" on any correct answer even when the ledger recorded IN_PROGRESS. It now speaks from the recorded status. The full backend suite is expected green after this change.
- Small-screen classroom (approved-interface acceptance "smaller screens reflow accessibly"): at 375 px there is no horizontal overflow, but the board began about 900 px down, below the textbook panel. The panel and board now carry one-tap jump links both ways (scrolling the in-page layout container, since fragment navigation alone does not move it) and the scan is capped at 38 vh on phones. Covered by a studio spec and checked in the emulated mobile viewport.
- Performance: page evidence responses shrank from about 2.4 MB to 10 KB by moving the scan to a cacheable image endpoint (public immutable for built-in books, signed and expiring for private uploads); evidence throughput rose from 53 to 234 requests per second on the dev server and the image is fetched once per source revision per browser. OCR results are persisted per source revision. Numbers and caveats in docs/evaluation/load-test-2026-09-11.md.
- Spaced repetition for practised pages (PRD 5.1.6): completed lesson runs schedule reviews at 1, 2, 7 and 21 days by stage; independent runs move up, assisted runs move down; due reviews appear on the parent dashboard with an open link and the classroom explains the schedule after a run. Kept separate from canonical mastery. Covered by policy, runtime, activity and dashboard specs.
- Teaching methodology (requested 11 September 2026): lessons now follow an enforced teacher's blueprint with phases (hook, prior knowledge, explain in the Experience-to-Symbol order, worked example, guided practice, independent check, misconception, transfer, reflection, summary), per-depth cognitive demands including first-principles inquiry at deep, ungraded prior-knowledge and reflection asks, and learner-knowledge grounding from the ledger (prior pages, known concepts, earlier misconceptions, suggested depth). Enforced by the validator, covered by unit tests on both sides. Not yet generated live: the Gemini daily quota was exhausted, so the first `guru-v3` lesson and its educator review are pending.
- Real-life examples in depth (requested 11 September 2026): the blueprint now requires a "From your life" phase at every depth, placed after the concept is introduced and before the worked example, with concrete, current, local examples explained in depth (what happens, why, how each part maps to the page's words), at least two explain-phase actions, and the drawing before any symbol or worked example. Opening a page already loads the lesson automatically for a signed-in learner. Enforced by the validator; the first live lesson under this rule awaits quota.
